## 1. Введение
   - 1.1. Актуальность темы (почему дискретная оптимизация важна в программировании и реальной жизни, примеры применения).
   - 1.2. Цели и задачи курсовой работы (разработка программы для визуализации алгоритмов, реализация конкретных задач и методов).
   - 1.3. Обзор выбранных задач (краткое описание задачи о рюкзаке и поиска кратчайшего пути).
   - 1.4. Обзор используемых алгоритмов (жадный, полный перебор, динамическое программирование).
   - 1.5. Методы визуализации и интерфейс (обзор инструментов, таких как PyQt или tkinter для GUI, и matplotlib для графиков).
   - 1.6. Структура курсовой работы (краткий обзор разделов).

## 2. Теоретические основы дискретной оптимизации
   - 2.1. Определение дискретной оптимизации (что это такое, отличие от непрерывной оптимизации).
   - 2.2. Классификация задач дискретной оптимизации (комбинаторные задачи, графовые задачи).
   - 2.3. Обзор методов решения:
      - 2.3.1. Жадные алгоритмы (принцип работы, преимущества и недостатки).
      - 2.3.2. Полный перебор (brute force: когда применяется, вычислительная сложность).
      - 2.3.3. Динамическое программирование (основные идеи, мемоизация, применимость).
   - 2.4. Анализ сложности алгоритмов (O-нотация для каждого метода).

## 3. Описание выбранных задач
   - 3.1. Задача о рюкзаке (0/1 knapsack problem):
      - 3.1.1. Формальная постановка задачи (предметы с весами и ценностями, ограничение по весу рюкзака).
      - 3.1.2. Примеры входных данных (маленький набор предметов для демонстрации).
      - 3.1.3. Применимые алгоритмы (жадный, brute force, динамическое программирование).
   - 3.2. Задача поиска кратчайшего пути (алгоритм Дейкстры):
      - 3.2.1. Формальная постановка задачи (граф с взвешенными ребрами, поиск пути от стартовой вершины).
      - 3.2.2. Примеры входных данных (простой граф с 4-5 вершинами).
      - 3.2.3. Применимые алгоритмы (жадный как основа Дейкстры, brute force для сравнения, динамическое программирование если адаптировать).

## 4. Структура проекта
   - 4.1. Общая архитектура программы (модульный подход: разделение на классы для задач, алгоритмов и визуализации).
   - 4.2. Предлагаемая структура файлов и модулей (на Python, так как проще для визуализации; если нужно на C++, адаптируем):
      - main.py — основной файл для запуска интерфейса.
      - knapsack.py — модуль для задачи о рюкзаке (функции для алгоритмов).
      - shortest_path.py — модуль для поиска кратчайшего пути (функции для алгоритмов).
      - visualization.py — модуль для визуализации шагов (используя matplotlib для графиков и таблиц).
      - gui.py — модуль для графического интерфейса (используя PyQt или tkinter: кнопки выбора задачи/алгоритма, поле для ввода данных, область визуализации).
      - utils.py — вспомогательный модуль (функции для ввода/вывода данных, обработки ошибок).
   - 4.3. Зависимости и библиотеки (matplotlib для графиков, PyQt5 для GUI; объяснение установки).
   - 4.4. Оптимальный порядок реализации (шаговый план):
      - Шаг 1: Реализовать алгоритмы без интерфейса (тестировать в консоли).
      - Шаг 2: Добавить визуализацию шагов (текстовые схемы или простые графики).
      - Шаг 3: Создать GUI и интегрировать всё.
      - Шаг 4: Тестирование и отладка.
      - Шаг 5: Документация и выводы (чтобы уложиться в срок, выделить 1-2 недели на каждый шаг).

## 5. Реализация алгоритмов
   - 5.1. Реализация для задачи о рюкзаке:
      - 5.1.1. Жадный алгоритм (пример кода на C++ с комментариями: сортировка по ценности/весу, выбор предметов).
      - 5.1.2. Полный перебор (brute force: рекурсия для всех комбинаций, пример кода).
      - 5.1.3. Динамическое программирование (таблица DP, пример кода с заполнением матрицы).
   - 5.2. Реализация для задачи поиска кратчайшего пути:
      - 5.2.1. Жадный алгоритм (Дейкстра: приоритетная очередь, пример кода с использованием std::priority_queue).
      - 5.2.2. Полный перебор (brute force: перебор всех путей, пример кода с рекурсией).
      - 5.2.3. Динамическое программирование (адаптация для графов без отрицательных весов, если применимо; пример кода).
   - 5.3. Примеры кода (для каждого алгоритма: полный код с комментариями, вход/выход, объяснение для начинающих).

## 6. Визуализация работы алгоритмов
   - 6.1. Общие принципы визуализации (пошаговое отображение: изменения состояний, таблицы, графики).
   - 6.2. Идеи для задачи о рюкзаке:
      - Текстовые схемы (консольный вывод таблицы DP или списка выбранных предметов).
      - Графическая визуализация (matplotlib: бар-чарт для весов/ценностей, анимация заполнения рюкзака; PyQt: таблица с подсветкой шагов).
   - 6.3. Идеи для задачи поиска кратчайшего пути:
      - Текстовые схемы (вывод графа в виде матрицы, шаги обновления расстояний).
      - Графическая визуализация (matplotlib: отрисовка графа с вершинами/ребрами, анимация пути; PyQt: интерактивный граф с подсветкой посещенных вершин).
   - 6.4. Интеграция визуализации в программу (функции для генерации изображений или обновления GUI на каждом шаге).

## 7. Разработка графического интерфейса
   - 7.1. Требования к интерфейсу (выбор задачи, алгоритма, ввод данных, кнопка "Запустить", область для визуализации).
   - 7.2. Выбор фреймворка (PyQt для более красивого GUI или tkinter для простоты; объяснение преимуществ).
   - 7.3. Описание элементов интерфейса (меню, поля ввода, canvas для графиков).
   - 7.4. Пример кода интерфейса (простой код на Python с PyQt: окно с кнопками и QLabel для вывода визуализации).
   - 7.5. Интеграция с алгоритмами (вызов функций из модулей при нажатии кнопки, пошаговая визуализация).

## 8. Тестирование и анализ результатов
   - 8.1. Методы тестирования (unit-тесты для алгоритмов, ручное тестирование интерфейса).
   - 8.2. Примеры тестовых данных (малые и большие наборы для каждой задачи).
   - 8.3. Сравнение алгоритмов (время выполнения, точность решений; таблицы результатов).
   - 8.4. Обработка ошибок (проверки ввода, исключения).

## 9. Заключение
   - 9.1. Итоги работы (что реализовано, достигнутые цели).
   - 9.2. Преимущества и недостатки разработанной программы.
   - 9.3. Возможные улучшения (добавление других задач, оптимизация визуализации).
   - 9.4. Список использованной литературы (книги по алгоритмам, документация библиотек).

## Приложения
   - А. Полные исходные коды программы (все файлы из структуры проекта).
   - Б. Скриншоты интерфейса и визуализации (примеры работы программы).
   - В. Таблицы сравнения алгоритмов (для разных размеров входных данных).